import {animated} from 'react-spring'
import {MixingValue} from 'react-mixing/src'

export default function () {
    if (typeof window === 'undefined')
        return null
    return (
      <animated.div>MixingValue</animated.div>
    )
}

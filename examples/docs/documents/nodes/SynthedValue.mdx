---
sidebar_position: 1
sidebar_label: SynthedValue
---

# SynthedValue


import {Live} from '../../components/Live'
import {SynthedValue} from 'react-mixing/src'

<Live.Card scope={{React, SynthedValue}} code={`
const osc = SynthedValue()
const vca = SynthedValue()
const dly = SynthedValue()
const handle = () => {
    const ctx = new AudioContext()
    osc.to(vca.to(dly.to()).to(), true)
}
render (
  <button onClick={handle}>
    start
  </button>
)
`.trim()}/>

- `number`

```js
new SynthedValue(0)
```

- `string`

```js
new SynthedValue(`0px 10px`)
```

- named nodes

```js
new SynthedValue('noise')
```

## Methods

*`get(): T`*

*`set (value): this`*

*`start(props?): Promise`*

*`stop(): this`*

*`reset(): this`*

## Properties

*`synthesis`*

*`queue`*
